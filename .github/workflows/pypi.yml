# This workflow will upload a Python Package using Twine when a release is created
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python#publishing-to-package-registries

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Upload Python Package

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish to PyPI'
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64]
    steps:
      - uses: actions/checkout@v7
      - name: Setup Build Environment
        uses: ./.github/actions/setup-build-env
        with:
          python-version: '3.12'
          install-cargo-tools: 'true'
          install-python-deps: 'false'
          cache-venv: 'false'

      - name: Prepare wheel bundle
        run: |
          make frontend
          make wheel-bundle nccl-profiler-lib

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --zig --compatibility manylinux2014 --auditwheel repair --features extension-module,gpu,gpu-cuda,kmsg
          sccache: "true"
          manylinux: auto
          rust-toolchain: stable

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-linux-${{ matrix.target }}
          path: dist

  macos:
    runs-on: macos-latest
    strategy:
      matrix:
        target: [x86_64, aarch64]
    steps:
      - uses: actions/checkout@v7
      - name: Setup Build Environment
        uses: ./.github/actions/setup-build-env
        with:
          python-version: '3.12'
          install-cargo-tools: 'true'
          install-python-deps: 'false'
          cache-venv: 'false'

      - name: Prepare wheel bundle
        run: |
          make frontend
          make wheel-bundle

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --find-interpreter --features extension-module,gpu,kmsg
          sccache: "true"
          rust-toolchain: stable

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-macos-${{ matrix.target }}
          path: dist

  windows:
    runs-on: windows-latest
    strategy:
      matrix:
        target: [x64]
    steps:
      - uses: actions/checkout@v7
      - name: Setup Build Environment
        uses: ./.github/actions/setup-build-env
        with:
          python-version: '3.12'
          install-cargo-tools: 'true'
          install-python-deps: 'false'
          cache-venv: 'false'
          enable-zig: 'false'

      - name: Prepare wheel bundle
        shell: bash
        run: |
          rm -rf python/probing/bundled_web
          cd web && dx bundle --release
          test -f python/probing/bundled_web/public/index.html
          mkdir -p python/probing/bundled_web/public/assets
          cp -f web/assets/logo.svg python/probing/bundled_web/public/logo.svg 2>/dev/null || true
          cp -f web/assets/logo.svg python/probing/bundled_web/public/assets/logo.svg 2>/dev/null || true
          cp -f web/assets/tailwind.css python/probing/bundled_web/public/assets/tailwind.css
          test -f python/probing/bundled_skills/catalog.yaml

      - name: Build wheels
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --find-interpreter --features extension-module,gpu,gpu-cuda,kmsg
          sccache: "true"
          rust-toolchain: stable

      - name: Upload wheels
        uses: actions/upload-artifact@v7
        with:
          name: wheels-windows-${{ matrix.target }}
          path: dist

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Build Environment
        uses: ./.github/actions/setup-build-env
        with:
          install-python-deps: 'false'
          install-cargo-tools: 'true'
          cache-venv: 'false'

      - name: Prepare wheel bundle
        run: |
          make frontend
          make wheel-bundle nccl-profiler-lib

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist --features extension-module,gpu,kmsg
          rust-toolchain: stable

      - name: Upload sdist
        uses: actions/upload-artifact@v7
        with:
          name: wheels-sdist
          path: dist

  deploy:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [linux, macos, windows, sdist]
    if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.publish == true)
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}
