# Workspace pages → suggested skills (Investigate page awareness).
apiVersion: probing.dev/v1
kind: PageCatalog

pages:
  dashboard:
    title: Dashboard
    path: /
    description: "Live CPU/GPU metrics, process overview, thread ranking."
    skills: [health_overview]

  agent:
    title: Investigate
    path: /agent
    description: "Skill-driven investigation; optional LLM diagnostic agent summarizes SQL evidence."
    skills: [health_overview, job_health]

  cluster:
    title: Cluster
    path: /cluster
    description: "Distributed training cluster nodes and rank status."
    skills: [slow_rank, comm_bottleneck, job_health, persistent_straggler]

  stacks:
    title: Stacks
    path: /stacks
    description: "Live mixed Python + native call stacks."
    skills: [training_hang]

  profiling/pprof:
    title: "Profiling · pprof"
    path: /profiling/pprof
    description: "CPU SIGPROF sampling flamegraph."
    skills: [training_hang, module_bottleneck]

  profiling/torch:
    title: "Profiling · torch"
    path: /profiling/torch
    description: "PyTorch module hook flamegraph (python.torch_trace)."
    skills: [module_bottleneck, memory_leak]

  profiling/trace:
    title: "Profiling · trace"
    path: /profiling/trace
    description: "Chrome trace event timeline."
    skills: [module_bottleneck]

  analytics:
    title: Analytics
    path: /analytics
    description: "Ad-hoc SQL analytics and time-series charts."
    skills: [health_overview, job_health, comm_bottleneck, slow_rank, persistent_straggler]

  spans:
    title: Spans
    path: /spans
    description: "Hierarchical tracing spans from python.trace_event."
    skills: [training_hang, comm_bottleneck]

  training:
    title: Training
    path: /training
    description: "Training step metrics and collective communication latency."
    skills: [job_health, slow_rank, persistent_straggler, comm_bottleneck, module_bottleneck]

  pulsing:
    title: Pulsing
    path: /pulsing
    description: "Ray/Pulsing actors and span capture."
    skills: []

  python:
    title: Python
    path: /python
    description: "Live Python variable tracing on functions."
    skills: []
